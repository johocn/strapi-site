# 文章页「权威依据/文中涉及」信任信息改造实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 C 端文章页「权威依据」「文中涉及」从机器校验元数据改造成用户可读的信任信息：总述 + 折叠 + 段落绑定 + 通用概念过滤；JSON-LD 补全完整元数据实现 AI/用户内容分层。

**Architecture:** 三仓联动。后端 basic（zhao-website 插件）为 geo-article 新增 `truthBasisSections` JSON 字段（claimKey→正文段落）；管理端 web 在文章编辑页为每条真值声明提供段落绑定 UI；C 端 strapi-site 重构 TruthBasis（总述+折叠+段落标注）、EntityMentions（标签云+黑名单过滤）、geo-seo（JSON-LD 补全）。

**Tech Stack:** Strapi v5 插件（zhao-website）、uni-app（web 管理端）、Next.js 静态导出（strapi-site）

---

### Task 1: 后端 schema 新增 truthBasisSections 字段

**Files:**
- Modify: `E:\code\basic\plugins\zhao-website\server\src\content-types\geo-article\schema.json`

- [ ] **Step 1: 在 schema.json 中新增字段**

在 `E:\code\basic\plugins\zhao-website\server\src\content-types\geo-article\schema.json` 的 `attributes` 对象内（任意位置，建议放在 `reviewNote`/`status` 附近）新增：

```json
"truthBasisSections": {
  "type": "json",
  "default": [],
  "description": "真值声明绑定正文段落：[{claimKey, section}]，section 为正文 H2 标题（如『二、长期学习规划四步法』）"
}
```

- [ ] **Step 2: 本地重建 zhao-website 插件 dist**

在 `E:\code\basic\plugins\zhao-website\` 目录执行：

```bash
npm run build
```

预期：`E:\code\basic\plugins\zhao-website\dist\server\content-types\geo-article\schema.json` 包含 `truthBasisSections`。自检：`Grep dist/server 含 truthBasisSections`。

- [ ] **Step 3: 提交并推送 basic 仓库**

```bash
git -C E:\code add plugins/zhao-website/server/src/content-types/geo-article/schema.json plugins/zhao-website/dist
git -C E:\code commit -m "feat(zhao-website): geo-article 新增 truthBasisSections 字段(真值声明绑定正文段落)"
git -C E:\code push
```

- [ ] **Step 4: 部署到生产（joho）**

按既有插件部署流程：服务器 `git pull` 更新 basic，重启 strapi（注意 2G 内存禁止服务器构建，dist 已本地构建并随 git 推送）。

验证（临时脚本 scp + ssh bash 执行）：
```bash
ssh joho "curl -s http://127.0.0.1:1337/api/zhao-website/v1/geo-articles/<slug> | grep -o truthBasisSections"
```
预期命中 `truthBasisSections`（未登录访问应为 401/403 而非 404——用管理端 token 或本地 curl 走 /api 前缀验证）。

- [ ] **Step 5: Commit 部署验证结果记录（无需 commit，跳过）**

---

### Task 2: 管理端 edit.vue 真值声明段落绑定

**Files:**
- Modify: `E:\code\web\src\pages\website\geo-article\edit.vue`

- [ ] **Step 1: form 增加 truthBasisSections 状态**

在 `form.value` 初始化处（约 L421 `truthBasis: []` 附近）新增：

```js
truthBasis: [],
truthBasisSections: [],
```

在 loadDetail 回显处（约 L584 `truthBasis: (item.truthBasis || []).map(t => t.id),` 后）新增：

```js
truthBasisSections: (item.truthBasisSections || []).filter(s => s && s.claimKey),
```

- [ ] **Step 2: 新增段落解析工具与绑定映射**

在 script 中（`parseJson` 函数附近）新增：

```js
// 从正文 content 提取 H2/H3 标题作为可绑定段落（含「开篇」「结语」）
const BASE_SECTIONS = ['开篇', '结语']
function extractContentSections(content) {
  const re = /<h([23])[^>]*>(.*?)<\/h\1>/gi
  const out = []
  let m
  while ((m = re.exec(content || '')) !== null) {
    const text = m[2].replace(/<[^>]+>/g, '').trim()
    if (text) out.push(text)
  }
  return out
}
// 计算可绑定的段落选项（正文标题 + 基础选项）
const sectionOptions = computed(() => {
  const fromBody = extractContentSections(form.value.content || '')
  return [...BASE_SECTIONS, ...fromBody]
})
// 绑定映射：claimKey → section（用于提交与回显）
const sectionMap = ref({})
watch(() => form.value.truthBasisSections, (list) => {
  const map = {}
  for (const s of list || []) if (s && s.claimKey) map[s.claimKey] = s.section
  sectionMap.value = map
}, { immediate: true })
// 保存某条声明的绑定段落
function setTruthSection(item, section) {
  const key = item.claimKey
  const list = [...form.value.truthBasisSections]
  const idx = list.findIndex(s => s && s.claimKey === key)
  if (section) {
    const entry = { claimKey: key, section }
    if (idx >= 0) list[idx] = entry; else list.push(entry)
  } else if (idx >= 0) {
    list.splice(idx, 1)
  }
  form.value.truthBasisSections = list
}
```

需确认 `computed`/`watch` 已 import（页面顶部 `import { ref, reactive, computed, watch, onLoad } from '@dcloudio/uni-app'`）。

- [ ] **Step 3: 真值声明 UI 增加段落绑定**

将「真值声明」form-item（约 L99-113）改为：勾选 chip 保持不变，其下新增已勾选声明的段落绑定列表：

```html
<view class="form-item">
  <text class="form-label">真值声明 *（至少 1 条，勾选后可绑定正文段落）</text>
  <view class="chip-group">
    <view
      v-for="item in truthList"
      :key="item.id"
      class="chip"
      :class="{ active: form.truthBasis.includes(item.id) }"
      @click="toggleChip(form.truthBasis, item.id)"
    >
      <text>{{ item.claim || `真值 #${item.id}` }}</text>
    </view>
    <text v-if="truthList.length === 0" class="chip-empty">暂无真值声明，请先在「第一真值」中创建</text>
  </view>

  <!-- 已勾选声明的段落绑定 -->
  <view v-if="selectedTruths.length" class="section-bind">
    <view v-for="item in selectedTruths" :key="item.id" class="section-bind-row">
      <text class="section-bind-claim">{{ item.claim }}</text>
      <picker mode="selector" :range="sectionOptions" @change="(e) => setTruthSection(item, sectionOptions[e.detail.value])">
        <view class="form-input picker-value">
          <text>{{ sectionMap[item.claimKey] || '选择绑定段落' }}</text>
          <text class="arrow">▼</text>
        </view>
      </picker>
    </view>
  </view>
</view>
```

并在 script 中新增计算属性（`truthList` 定义后）：

```js
const selectedTruths = computed(() =>
  truthList.value.filter(t => form.value.truthBasis.includes(t.id))
)
```

- [ ] **Step 4: 新增 section-bind 样式**

在 `<style scoped>` 中新增：

```css
.section-bind { margin-top: 16rpx; }
.section-bind-row {
  display: flex; align-items: center; gap: 16rpx;
  padding: 12rpx; margin-top: 8rpx;
  background: #fafafa; border-radius: 8rpx;
}
.section-bind-claim { flex: 1; font-size: 26rpx; color: #333; }
```

- [ ] **Step 5: 构建验证 web**

```bash
npm run build:h5 --prefix E:\code\web
```
预期构建成功（无语法错误）。

- [ ] **Step 6: 提交推送 web 仓库**

```bash
git -C E:\code\web add src/pages/website/geo-article/edit.vue
git -C E:\code\web commit -m "feat(web): 文章编辑页真值声明支持绑定正文段落(truthBasisSections)"
git -C E:\code\web push
```

---

### Task 3: C 端类型扩展

**Files:**
- Modify: `E:\code\strapi-site\lib\geo-article.ts`

- [ ] **Step 1: GeoArticle 类型补充字段**

`truthBasis` 项类型补充 `claimKey`/`claimCategory`，并新增 `truthBasisSections`：

```ts
truthBasis?: {
  id: number;
  claimKey?: string;
  claim: string;
  claimCategory?: string;
  canonicalValue?: string;
  canonicalSourceUrl?: string;
  canonicalSourceType?: string;
  verificationStatus?: string;
  lastVerifiedAt?: string;
}[];
truthBasisSections?: { claimKey?: string; section?: string }[];
```

- [ ] **Step 2: Commit**

```bash
git -C E:\code add strapi-site/lib/geo-article.ts
git -C E:\code commit -m "feat(strapi-site): GeoArticle 类型补充 truthBasisSections/claimKey"
```

---

### Task 4: C 端 TruthBasis 组件重构（总述+折叠+段落标注）

**Files:**
- Modify: `E:\code\strapi-site\components\modules\geo\TruthBasis.tsx`

- [ ] **Step 1: 重写组件**

替换文件全部内容为：

```tsx
import type { GeoArticle } from "@/lib/geo-article";

const STATUS_LABEL: Record<string, string> = {
  verified: "已验证",
  pending: "待验证",
  outdated: "已过期",
  conflict: "存在冲突",
};

/** 默认展示条数（无绑定数据时的兜底） */
const FALLBACK_VISIBLE = 3;

/**
 * GEO 模块：权威依据（文章背书的真值声明）。
 * 用户侧：一行总述 + 折叠详情（仅已绑定正文段落的依据，标注对应段落）。
 * AI 侧：全部真值声明仍在 SSR HTML 中（含未绑定/通用定义），SEO 爬虫可抓取完整数据。
 */
export default function TruthBasis({ article }: { article: GeoArticle }) {
  const basis = article.truthBasis ?? [];
  if (basis.length === 0) return null;

  const sections = article.truthBasisSections ?? [];
  const sectionByKey = new Map(
    sections.filter((s) => s?.claimKey && s?.section).map((s) => [s.claimKey, s.section])
  );
  // 用户可见：已绑定段落的依据（按绑定顺序优先，未绑定排后）
  const bound = basis.filter((t) => t.claimKey && sectionByKey.has(t.claimKey));
  const unbound = basis.filter((t) => !(t.claimKey && sectionByKey.has(t.claimKey)));
  const visible = bound.length > 0 ? bound : unbound.slice(0, FALLBACK_VISIBLE);
  const summarySource = article.sourceName || "相关权威政策文件";
  const summaryCount = bound.length > 0 ? bound.length : basis.length;

  return (
    <section className="geo-truth-basis">
      <h2>权威依据</h2>
      <p className="geo-truth-summary">
        本文观点参考{summarySource}，配套 {summaryCount} 条核心依据。
      </p>
      <details className="geo-collapse">
        <summary className="geo-collapse-summary">
          {bound.length > 0 ? `查看全部依据（${basis.length} 条）` : `查看真值声明（${basis.length} 条）`}
        </summary>
        <ul>
          {visible.map((t) => {
            const section = t.claimKey ? sectionByKey.get(t.claimKey) : undefined;
            return (
              <li key={t.id} className="geo-truth-item">
                {section && <div className="geo-truth-section">对应：{section}</div>}
                <div className="geo-truth-claim">{t.claim}</div>
                {t.canonicalValue && <div className="geo-truth-value">权威值：{t.canonicalValue}</div>}
                <div className="geo-truth-meta">
                  {t.canonicalSourceUrl ? (
                    <a href={t.canonicalSourceUrl} target="_blank" rel="noopener noreferrer">查看来源</a>
                  ) : t.canonicalSourceType ? (
                    <span>来源类型：{t.canonicalSourceType}</span>
                  ) : null}
                  {t.verificationStatus && (
                    <span className={`geo-truth-badge geo-truth-${t.verificationStatus}`}>
                      {STATUS_LABEL[t.verificationStatus] ?? t.verificationStatus}
                    </span>
                  )}
                </div>
              </li>
            );
          })}
          {/* 未绑定依据：保持完整数据供爬虫，视觉上弱化折叠 */}
          {unbound.length > 0 && bound.length > 0 && (
            <li className="geo-truth-item geo-truth-unbound">
              <details className="geo-collapse-inner">
                <summary>另有 {unbound.length} 条支撑声明（点击展开）</summary>
                {unbound.map((t) => (
                  <div key={t.id} className="geo-truth-claim">{t.claim}</div>
                ))}
              </details>
            </li>
          )}
        </ul>
      </details>
    </section>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git -C E:\code add strapi-site/components/modules/geo/TruthBasis.tsx
git -C E:\code commit -m "feat(strapi-site): 权威依据改为总述+折叠+段落绑定标注，未绑定弱化展示"
```

---

### Task 5: C 端 EntityMentions 组件重构（标签云+黑名单过滤）

**Files:**
- Modify: `E:\code\strapi-site\components\modules\geo\EntityMentions.tsx`

- [ ] **Step 1: 重写组件**

替换文件全部内容为：

```tsx
import type { GeoArticle } from "@/lib/geo-article";

/** 通用概念词（DefinedTerm 基础概念，用户不可见；AI/爬虫仍可在 HTML 中抓取） */
const GENERIC_ENTITY_WORDS = ["教育", "学习", "课程", "在线教育", "在线课程", "学习方法", "学习资源", "学历教育", "考试认证"];

/** 过滤：DefinedTerm 且命中通用概念词 → 用户不可见 */
function isGeneric(entity: { name?: string; entityType?: string }): boolean {
  if (entity.entityType !== "DefinedTerm") return false;
  return GENERIC_ENTITY_WORDS.includes(entity.name || "");
}

/**
 * GEO 模块：实体关联（文中提及的知识图谱实体，标签云展示强相关实体）。
 * 用户侧：只展示过滤后的强相关实体（标签 chip）。
 * AI 侧：全部实体仍在 SSR HTML 中，SEO 爬虫可抓取完整数据。
 */
export default function EntityMentions({ article }: { article: GeoArticle }) {
  const entities = article.mentionedEntities ?? [];
  const visible = entities.filter((e) => !isGeneric(e));
  if (visible.length === 0) return null;
  return (
    <section className="geo-entity-mentions">
      <h2>文中涉及</h2>
      <details className="geo-collapse">
        <summary className="geo-collapse-summary">
          查看涉及概念（{visible.length} 个）
        </summary>
        <ul className="geo-entity-tags">
          {visible.map((e) => (
            <li key={e.id} className="geo-entity-tag">
              <span className="geo-entity-name">{e.name}</span>
              {e.entityType && <span className="geo-entity-type">{e.entityType}</span>}
            </li>
          ))}
        </ul>
        {/* 通用概念保持完整数据供爬虫，视觉隐藏 */}
        {entities.filter((e) => isGeneric(e)).length > 0 && (
          <div className="geo-entity-generic" aria-hidden="true">
            {entities.filter((e) => isGeneric(e)).map((e) => (
              <span key={e.id} className="geo-entity-generic-item">{e.name}</span>
            ))}
          </div>
        )}
      </details>
    </section>
  );
}
```

- [ ] **Step 2: globals.css 新增标签云样式**

在 `E:\code\strapi-site\app\globals.css` 中（`.geo-truth-*` 相关样式附近）新增：

```css
/* 权威依据：总述与段落标注 */
.geo-truth-summary {
  font-size: 14px;
  color: #4b5563;
  background: #f9fafb;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  padding: 10px 14px;
  margin: 8px 0;
}
.geo-truth-section {
  font-size: 12px;
  color: var(--color-primary);
  font-weight: 500;
  margin-bottom: 4px;
}
.geo-truth-unbound { margin-top: 8px; opacity: 0.75; }
.geo-collapse-inner summary {
  font-size: 13px;
  color: #6b7280;
  cursor: pointer;
}
.geo-collapse-inner .geo-truth-claim { padding: 4px 0; }

/* 文中涉及：标签云 */
.geo-entity-tags {
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 0;
  padding: 0;
}
.geo-entity-tag {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  background: #f3f4f6;
  border: 1px solid #e5e7eb;
  border-radius: 999px;
  font-size: 13px;
  color: #374151;
}
.geo-entity-tag .geo-entity-type {
  font-size: 11px;
  color: #9ca3af;
}
/* 通用概念：仅保留给爬虫，视觉隐藏 */
.geo-entity-generic { display: none; }
```

- [ ] **Step 3: Commit**

```bash
git -C E:\code add strapi-site/components/modules/geo/EntityMentions.tsx strapi-site/app/globals.css
git -C E:\code commit -m "feat(strapi-site): 文中涉及改为标签云+通用概念黑名单过滤，AI 数据保留"
```

---

### Task 6: C 端 geo-seo JSON-LD 补全（AI 内容分层）

**Files:**
- Modify: `E:\code\strapi-site\lib\geo-seo.ts`

- [ ] **Step 1: buildGeoJsonLd 补充 mentions/citation**

在 `buildGeoJsonLd` 的 `base` 对象后、各类型 return 前（约 L19 后），注入完整元数据：

```ts
const mentions =
  (article.mentionedEntities ?? [])
    .map((e) => ({ "@type": "DefinedTerm", name: e.name, ...(e.slug ? { url: `${SITE_URL}/knowledge/${e.slug}` } : {}) }))
    .filter((m) => m.name);
const citation = (article.truthBasis ?? [])
  .map((t) => ({
    "@type": "CreativeWork",
    name: t.claim,
    ...(t.canonicalSourceUrl ? { url: t.canonicalSourceUrl } : {}),
    ...(t.verificationStatus === "verified" ? { review: { "@type": "Review", reviewRating: { "@type": "Rating", ratingValue: "5", bestRating: "5" } } } : {}),
  }))
  .filter((c) => c.name);
```

并将 `base` 返回处改为：

```ts
return {
  ...base,
  ...(mentions.length ? { mentions } : {}),
  ...(citation.length ? { citation } : {}),
};
```

注意：`FAQPage`/`LocalBusiness`/`ItemList` 分支同样需要带上 mentions/citation，将上述 return 改为合并对象：

```ts
const withMeta = (extra: Record<string, unknown>) => ({
  ...base,
  ...extra,
  ...(mentions.length ? { mentions } : {}),
  ...(citation.length ? { citation } : {}),
});
if (type === "FAQPage") return withMeta({ mainEntity: [...] });
if (type === "LocalBusiness") return withMeta({ ... });
if (type === "ItemList") return items.length ? withMeta({ name: site?.siteName, itemListElement: items }) : null;
return withMeta({});
```

（原各分支的 `...base` 用 `withMeta` 替代，业务字段放 `extra`。）

- [ ] **Step 2: Commit**

```bash
git -C E:\code add strapi-site/lib/geo-seo.ts
git -C E:\code commit -m "feat(strapi-site): JSON-LD 补全 mentions/citation 完整元数据(AI 内容分层)"
```

---

### Task 7: C 端构建 + 部署 + 线上验证

**Files:**
- 无（构建/部署）

- [ ] **Step 1: 清理 fetch-cache 并构建**

```bash
Remove-Item -Recurse -Force "E:\code\strapi-site\.next\cache\fetch-cache" -ErrorAction SilentlyContinue
npm run build --prefix E:\code\strapi-site
```

预期构建成功。

- [ ] **Step 2: 本地产物自检**

```bash
# 文章1（career-lifelong-learning-plan）产物
$c = Get-Content -Raw "E:\code\strapi-site\out\geo-article\career-lifelong-learning-plan.html"
# 总述存在
$c.Contains("本文观点参考")            # True
# 无「教育是」通用实体出现在标签可见区（检查是否仅剩隐藏 div）
# JSON-LD 含完整 mentions/citation
[regex]::Matches($c, '"@type":"DefinedTerm"').Count   # >= 16（全部实体）
```

- [ ] **Step 3: 部署 www.joho.cn**

```bash
powershell -ExecutionPolicy Bypass -File "E:\code\strapi-site\deploy-www.ps1"
```

预期输出以 `SYNC_OK` 结尾。

- [ ] **Step 4: 线上验证（文件存在性 + 内容）**

```bash
curl.exe -s --resolve www.joho.cn:443:39.97.54.5 "https://www.joho.cn/geo-article/career-lifelong-learning-plan" | Select-String "本文观点参考|查看涉及概念|DefinedTerm"
```

预期同时命中：总述文案、涉及概念标签、JSON-LD 完整实体（防 SPA 假 200，另用 ssh `ls` 线上 out 目录对比 _next buildId）。

- [ ] **Step 5: 验收核对 spec**

| 验收项 | 验证方式 |
|---|---|
| 权威依据默认只显示总述，展开仅含绑定段落依据且标注段落 | 线上 HTML grep `对应：` |
| 文中涉及仅强相关实体标签，无「教育」「学习」通用词 | 线上 HTML grep `geo-entity-tag` 列表 |
| 页面 HTML 含完整 truthBasis/entities，JSON-LD 含完整元数据 | grep `DefinedTerm` 计数 = 全部实体 |
| 管理端可绑定段落、回显正确 | web 端构建通过 + 部署后人工操作（需用户在管理后台验证） |
| 旧文章（无绑定）正常渲染 | 线上访问无绑定文章页 200 无报错 |

---

## Self-Review 备注

- **spec 覆盖**：Task1（§4.1 schema）、Task2（§5 管理端绑定 UI）、Task3-5（§6.1 用户侧）、Task6（§6.2 AI 侧 JSON-LD）、Task7（§8 验收）——全部覆盖。
- **类型一致性**：`truthBasisSections` 结构 `{claimKey, section}` 三端一致（schema.json / web form / C 端类型）；`claimKey` 来自 first-truth-policy 字段，与 web `truthList` 项一致。
- **部署铁律**：Task1 明确本地重建 dist 后推送（2G 服务器禁止构建）；web 构建走本机 `build:h5`；strapi-site 构建后 deploy-www.ps1。
- **风险**：管理端段落绑定 UI 用 uni-app picker，`sectionOptions` 为 computed；未绑定声明在 C 端以弱化折叠展示（保持 SEO 数据完整）。
